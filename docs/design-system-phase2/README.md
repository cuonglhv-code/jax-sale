# jax-sales design-system brief — Phase 2, for Claude (claude.ai)

Phase 1 (already built and live) covered the design system's tokens, the app shell, Tasks, and HR
Reports — see `design_handoff_jax_sales/` for that reference. This folder covers the 4 modules Phase 1
deliberately left as placeholders: Sales Performance/KPI, HR Requests, HR Approvals, and Timetable.

**Read in this order:**

1. **`design_handoff_jax_sales/README.md`** (sibling folder, from Phase 1) — the existing token system,
   shell, and two finished pages. Read this FIRST — Phase 2 must extend it exactly, not diverge.
2. **`00-BRIEF.md`** — this phase's scope: the 4 modules, what each needs, what to leave alone.
3. **`01-CODEBASE-CONTEXT.md`** — current (unstyled) state of each module, exact component shapes.
4. **`02-DELIVERABLE-SPEC.md`** — what to hand back and in what format.

## Suggested opening prompt

> I've attached a Phase 1 design reference (design_handoff_jax_sales/README.md — already built, read
> this first) plus a Phase 2 brief (00-BRIEF.md), codebase context (01-CODEBASE-CONTEXT.md), and a
> deliverable spec (02-DELIVERABLE-SPEC.md) for the next 4 pages of the same Vietnamese internal CRM
> tool. Please read all of it, then design these 4 modules using Phase 1's EXISTING tokens, shell, and
> component patterns — no new colors, fonts, or design language. Start with the token/pattern reuse
> confirmation, then mock the modules called out in the deliverable spec.
